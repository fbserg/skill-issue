This file is a stale, unmaintained fork. The live, deployed operating rules
are `/Users/serg/projects/etc/configs/claude/CLAUDE.md` (symlinked from
`~/.claude/CLAUDE.md`) — read that file directly rather than copying anything
from here. Do not install this file into a project or into `~/.claude/`; it
silently reverts rulings the live file carries.
